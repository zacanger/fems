* Should apps define an `unmount`?
* How do we actually handle mounting and unmounting?
  * `mount(app)` should happen when some boolean, unmount when not
* Can this work _with_ single-spa (is this a Sofe replacement)?
